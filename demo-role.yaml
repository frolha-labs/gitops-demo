kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: demo-pod-rw
  namespace: default
rules:
  - verbs:
      - get
      - watch
      - list
    apiGroups:
      - ''
    resources:
      - pods
